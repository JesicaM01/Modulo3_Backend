//Declaramos la funcion con sus dos tipos 
function multiplicarNumeros (a: number,b: number): number {
    return a * b;
}

//llamamso a la funcion y guardamos el resultado
let resultado = multiplicarNumeros(5, 8);

//mostramos en la consola
console.log("El resultado de la multiplicación es: " + resultado);